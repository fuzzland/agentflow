# Variant Analysis

Generalize each confirmed Solidity issue into nearby EVM variants.
Search for equivalent anti-patterns across inherited contracts, proxy implementations, and sibling modules.
Prefer structured checklists so each variant candidate is reviewed with the same evidence standard.
