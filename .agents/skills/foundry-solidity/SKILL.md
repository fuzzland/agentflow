# Foundry Solidity

Use Foundry as the primary Solidity workflow.
Run forge build, forge test, and forge test --gas-report when validating behavior and performance.
When reproducing findings, prefer minimal Foundry tests or scripts tied to exact contracts and compiler settings.
